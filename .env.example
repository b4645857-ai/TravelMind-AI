# Copy this file to .env and fill in your real keys. Never commit .env itself.

ANTHROPIC_API_KEY=your_key_here

# Optional: only needed if Person A wires up a live rail API (see data/train_data.py)
RAIL_API_KEY=
RAIL_API_BASE_URL=https://indianrailapi.com/api/v2

# Weather uses Open-Meteo which needs NO key — nothing to fill in there.
